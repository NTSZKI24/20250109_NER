import { ITodo } from "@/app/todos/page"
import { Col } from "react-bootstrap"

export default function TodosDisplay({
    todos,
    setTodos
} : {
    todos: ITodo[]
    setTodos: Function
}) {
    return (
        <Col xs={12}>
        
        </Col>
    )
}